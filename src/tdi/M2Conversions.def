DEFINITION MODULE M2Conversions;

FROM SYSTEM IMPORT WORD, ADDRESS;

(* 1 *) PROCEDURE ConvertCardinal(CARDINAL; CARDINAL; VAR ARRAY CHAR);

(* 2 *) PROCEDURE ConvertInteger(INTEGER; CARDINAL; VAR ARRAY CHAR);

(* 3 *) PROCEDURE ConvertReal(REAL; CARDINAL; INTEGER; VAR ARRAY CHAR);

(* 4 *) PROCEDURE ConvertOctal(CARDINAL; CARDINAL; VAR ARRAY CHAR);

(* 5 *) PROCEDURE ConvertHex(CARDINAL; CARDINAL; VAR ARRAY CHAR);

(* 6 *) PROCEDURE ConvertAddrDec(SYSTEM.ADDRESS; CARDINAL; VAR ARRAY CHAR);

(* 7 *) PROCEDURE ConvertAddrOct(SYSTEM.ADDRESS; CARDINAL; VAR ARRAY CHAR);

(* 8 *) PROCEDURE ConvertAddrHex(SYSTEM.ADDRESS; CARDINAL; VAR ARRAY CHAR);

(* 9 *) PROCEDURE ConvertToCardinal(VAR ARRAY CHAR; VAR BOOLEAN; VAR CARDINAL);

(* 10 *) PROCEDURE ConvertToInteger(VAR ARRAY CHAR; VAR BOOLEAN; VAR INTEGER);

(* 11 *) PROCEDURE ConvertToReal(VAR ARRAY CHAR; VAR BOOLEAN; VAR REAL);

(* 12 *) PROCEDURE ConvertToOctal(VAR ARRAY CHAR; VAR BOOLEAN; VAR WORD);

(* 13 *) PROCEDURE ConvertToHex(VAR ARRAY CHAR; VAR BOOLEAN; VAR WORD);

(* 14 *) PROCEDURE ConvertToAddr(VAR ARRAY CHAR; VAR BOOLEAN; VAR ADDRESS);
    
END M2Conversions.
