DEFINITION MODULE AESShells;

FROM SYSTEM IMPORT ADDRESS;

(* 1 *) PROCEDURE ShellRead(ADDRESS; ADDRESS);

(* 2 *) PROCEDURE ShellWrite(INTEGER; INTEGER; INTEGER; ADDRESS; ADDRESS);

(* 3 *) PROCEDURE ShellFind(ADDRESS);

(* 4 *) PROCEDURE ShellEnvrn(ADDRESS; ADDRESS);
    
END AESShells.
